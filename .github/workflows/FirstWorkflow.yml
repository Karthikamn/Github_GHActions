name: First Workflow

on: 
 workflow_dispatch
 
jobs:
 build:
  runs-on: ubuntu-latest
  steps:
   - name: Print
     run: echo 'hello World'
 test:
  runs-on: ubuntu-latest
  steps:
   - name: Display
     run: echo "test the step"
  
